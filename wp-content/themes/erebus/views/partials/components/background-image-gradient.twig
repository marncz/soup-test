<style>
    @media (min-width: 1440px) {
        .{{ class }} {
            background-image: linear-gradient(to bottom, rgba(0, 0, 0, 0), rgba(0, 0, 0, .5)),
            url({{ function('wp_get_attachment_image_url', image, image_size ~ '-jumbo') }});
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
        }
    }
    @media (max-width: 1440px) {
        .{{ class }} {
            background-image: linear-gradient(to bottom, rgba(0, 0, 0, 0), rgba(0, 0, 0, .5)),
            url({{ function('wp_get_attachment_image_url', image, image_size ~ '-desktop') }});
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
        }
    }
    @media (max-width: 768px) {
        .{{ class }} {
            background-image: linear-gradient(to bottom, rgba(0, 0, 0, 0), rgba(0, 0, 0, .5)),
            url({{ function('wp_get_attachment_image_url', image, image_size ~ '-tablet') }});
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
        }
    }
    @media (max-width: 360px) {
        .{{ class }} {
            background-image: linear-gradient(to bottom, rgba(0, 0, 0, 0), rgba(0, 0, 0, .5)),
            url({{ function('wp_get_attachment_image_url', image, image_size ~ 'full-width-mobile') }});
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
        }
    }
</style>